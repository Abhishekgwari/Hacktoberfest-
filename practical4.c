// to check number is even or odd
#include <stdio.h>
int main ()
{
int num;
printf("enter the number");
scanf("%d",&num);
if (num%2==3)
{
    printf("number is even so it is true");
    }
    else
    {
printf("number is odd so it is false");
  }
return 0;
}
